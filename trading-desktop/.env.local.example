# =============================================================================
# Trading Desktop - Environment Variables
# =============================================================================
# Copy this file to .env.local and adjust values for your local setup.
#
# These environment variables are loaded by Vite at build time. Variables
# prefixed with VITE_ are exposed to the client-side code.
# =============================================================================

# -----------------------------------------------------------------------------
# Market Spine Backend URL
# -----------------------------------------------------------------------------
# The base URL for the Market Spine API backend.
# The Vite dev server will proxy /v1/* requests to this URL.
#
# Examples:
#   - Basic tier:        http://localhost:8001
#   - Intermediate tier: http://localhost:8002
#   - Full tier:         http://localhost:8003
#
VITE_MARKET_SPINE_URL=http://localhost:8001

# -----------------------------------------------------------------------------
# Market Spine Profile Name
# -----------------------------------------------------------------------------
# Display name for the backend tier. Used in the UI to show which tier is
# connected. This is for display purposes only - the actual capabilities
# are determined by the /v1/capabilities endpoint response.
#
# Options: basic, intermediate, full
#
VITE_MARKET_SPINE_PROFILE=basic

# -----------------------------------------------------------------------------
# Mock Mode (Future)
# -----------------------------------------------------------------------------
# When enabled, the API client will return mock data instead of making
# real API calls. Useful for frontend development without a running backend.
#
# VITE_MOCK_MODE=false
