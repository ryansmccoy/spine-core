# Development Dockerfile for Frontend with Vite hot reload
FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package*.json ./
RUN npm ci

# Source code will be mounted at runtime
# No COPY of src - we mount for hot reload

# In Docker, the API is at http://api:8000
ENV VITE_MARKET_SPINE_URL=http://api:8000

EXPOSE 3000

# Vite dev server with host binding for Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
