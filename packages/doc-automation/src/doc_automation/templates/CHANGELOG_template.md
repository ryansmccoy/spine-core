# CHANGELOG

**Version History**

*Auto-generated from code annotations on {{ generated_at.strftime('%Y-%m-%d') }}*

---

All notable changes to this project are documented here, extracted from code annotations.

{% for version in versions %}
{% set entries = entries_by_version[version] %}
{% set has_breaking = entries | selectattr('breaking') | list | length > 0 %}
## v{{ version }}{% if has_breaking %} ⚠️ BREAKING{% endif %}

{% for entry in entries %}
{% if entry.breaking %}💥{% else %}-{% endif %} {{ entry.description }}
{% if entry.source_class %}  - *In {{ entry.source_class }}*{% endif %}
{% endfor %}

{% endfor %}

---

*{{ total_entries }} changes across {{ total_versions }} versions*

*Generated by [doc-automation](https://github.com/your-org/py-sec-edgar/tree/main/spine-core/packages/doc-automation)*
