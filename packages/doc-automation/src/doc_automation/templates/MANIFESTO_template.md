# MANIFESTO

**Core Principles and Philosophy**

> **Auto-generated from code annotations**  
> **Last Updated**: {{ generated_at.strftime('%B %Y') }}  
> **Status**: Living Document

---

## Table of Contents

{% set toc_num = 1 %}
{% for section_name in sections.keys() %}
{{ toc_num }}. [{{ section_name }}](#{{ section_name | lower | replace(' ', '-') }})
{% set toc_num = toc_num + 1 %}
{% endfor %}

---

## The Challenge

Financial data comes from many sources, each with their own identifiers.  
The core challenge is: *"Is this the same company across different sources?"*

---

{% for section_name, fragments in sections.items() %}
## {{ section_name }}

{% for fragment in fragments %}
{% set content = fragment.content if fragment.content is string else fragment.get('content', '') %}
{% if '┌' in content or '└' in content %}
```
{{ content | trim }}
```
{% else %}
{{ content | trim }}
{% endif %}

{% set source_class = fragment.source_class if fragment.source_class is defined else fragment.get('source_class') %}
{% set source_file = fragment.source_file if fragment.source_file is defined else fragment.get('source_file', '') %}
{% set source_line = fragment.source_line if fragment.source_line is defined else fragment.get('source_line') %}
{% if source_class %}
*Source: [`{{ source_class }}`]({{ source_file }}{% if source_line %}#L{{ source_line }}{% endif %})*
{% endif %}

{% endfor %}
{% endfor %}

---

*{{ total_fragments }} principles extracted from {{ total_sections }} sections*

*Generated by [doc-automation](https://github.com/your-org/py-sec-edgar/tree/main/spine-core/packages/doc-automation)*
