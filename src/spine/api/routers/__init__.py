"""API routers package.

Manifesto:
    Each router module owns one API domain (runs, alerts, sources,
    etc.) and delegates to ``spine.ops`` for business logic.

Tags:
    spine-core, api, routers, REST, endpoint-collection

Doc-Types:
    api-reference
"""
